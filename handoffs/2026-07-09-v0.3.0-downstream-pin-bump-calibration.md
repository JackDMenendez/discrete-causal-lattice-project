---
handoff: 2026-07-09-v0.3.0-downstream-pin-bump-calibration
from: PM (dcl-website session)
to: dcl-delta-p-min + dcl-paper-04 (focused)
repo: [JackDMenendez/dcl-delta-p-min, JackDMenendez/dcl-paper-04-optical-axis-birefringence]
branch: main
commits: []                         # directive; downstream sessions make the commits
pr: none
status: open
state: complete                     # directive fully specified; execution is each focused session's
semver: n/a (investigation / paper repos; committed dcl_core pin bump + internal unit-constant refactor)
flags:
  - "SHARED VENV ALREADY UPDATED: the canonical .venv-win now carries dcl_core v0.3.0 (user-confirmed 2026-07-09). No pip install is needed to RUN. But each repo MUST still bump its COMMITTED pin (virtual-env-requirements.txt / pyproject dependency) from its current v0.2.x to v0.3.0 so the reproduce recipe matches what is actually installed — a green run against an un-bumped pin is a reproducibility trap."
  - "CALIBRATION DISCIPLINE (the point of v0.3.0): `dcl_core.calibration` is now the ONE home for every physical-unit conversion (seconds/hz/joules/metres). Route unit conversions through it; do NOT reintroduce ad-hoc unit constants in experiment code. The boundary carries exactly ONE free debt (`tick_seconds`); dcl-core has a CI tripwire asserting that count. Do not smuggle a second scale into your own code as if it were free."
  - "MAJOR-WATCH (inherit, do not fight): `Calibration.length_metres` assumes the light-cone identity a = c*tick_seconds. Treat it as the v0.3.0 contract; if your physics needs lattice spacing set independently of c, that is a dcl-core MAJOR request via handoff, not a local override."
  - "SCOPE EXCLUSION: Paper VI / Bell pins `core` (continuous-amplitude, v0.2.2), which does NOT carry the calibration wall (deferred to core's next MAJOR). This directive does NOT touch Paper VI/Bell — leave that pin at v0.2.2."
  - "core vs core3d: the calibration wall lives in core3d. dcl-delta-p-min exercises BOTH engines; the pin bump to v0.3.0 is safe for both (additive, no break), and `dcl_core.calibration` is importable regardless of which engine an experiment uses — so ad-hoc constants can be centralised even in core-side experiment code."
decisions:
  - "One relay to two repos: the action shape is identical (bump pin + centralise unit constants), so it is filed as a single PM directive with per-repo consumer actions rather than two near-duplicate handoffs. Each focused session consumes the item addressed to its repo and can flip status when both are done, or split if one lands first."
consumed_by:
consumed_at:
---

## Summary
`dcl_core` **v0.3.0** is released (2026-07-08; tag `v0.3.0`, commit `3175700`,
doi:10.5281/zenodo.21272238). It adds the `dcl_core.calibration` boundary — the
single quarantine for physical-unit conversions — while keeping `core3d`
constant-free (0 engine constants, 1 free debt: `tick_seconds`). Per the release
directive, downstream consumers pick this up **when they bump their pin**: bump to
v0.3.0 and move any ad-hoc physical-unit constants in experiment code into
`dcl_core.calibration`. The shared `.venv-win` already has v0.3.0 installed, so this
is a committed-pin + refactor task, not an environment task. This handoff relays that
to the two affected repos. Paper VI/Bell is out of scope (pins `core` v0.2.2).

## Shipped
Nothing downstream yet — PM directive. Upstream (dcl-core, released):
- `v0.3.0` — `dcl_core.calibration` submodule (conversions from `tick_seconds` +
  CODATA `hbar`/`c`; `CALIBRATION_DEBTS`), U(1) Peierls gauge coupling in `core3d`,
  fused CuPy `hop_average` GPU path. Release notes: `release_notes/v0.3.0.md`.

## Verification
Upstream release-gate tests green at release: `test_peierls.py`,
`test_calibration_boundary.py` (18, incl. the one-debt tripwire + AST no-leak sweep),
`test_gpu_matches_cpu.py`. api-stability verdict MINOR (additive; no removals/renames).
Shared `.venv-win` confirmed carrying v0.3.0 (user, 2026-07-09). No downstream
verification yet — that is each focused session's job after the bump.

## Remaining
Each repo: bump pin, centralise unit constants, run its own suite against v0.3.0.
`core`-side calibration wall is deferred to `core`'s next MAJOR (not this directive).

## Decisions & flags
See frontmatter. Load-bearing: (1) bump the **committed** pin even though the venv is
already updated — otherwise the reproduce recipe silently lies; (2) `dcl_core.calibration`
is the one unit home, don't reintroduce ad-hoc constants; (3) inherit the
`a = c*tick_seconds` assumption rather than overriding it locally; (4) Paper VI/Bell
stays on `core` v0.2.2, untouched.

## → Consumer actions

**dcl-delta-p-min (Paper III / δp_min investigation):**
- [ ] Bump the committed `dcl_core` pin to `v0.3.0`
      (`virtual-env-requirements.txt` / pyproject dependency).
- [ ] Move the Bohr/hydrogen **calibration constants** used in the experiments into
      `dcl_core.calibration` (route seconds/energy/length conversions through it); do
      not keep ad-hoc unit constants in experiment code.
- [ ] Run the investigation suite against v0.3.0; confirm A=1 exactness and the δp_min
      sweep results are unchanged (additive release — expect no behavioural change).
- [ ] Do NOT change the Paper VI/Bell `core` v0.2.2 pin (scope-exclusion flag).

**dcl-paper-04 (optical-axis birefringence):**  ✅ DONE 2026-07-09 (commit 3de7423)
- [x] Bump the committed `dcl_core` pin to `v0.3.0` — repo had **no** pin file;
      created `virtual-env-requirements.txt` pinning `dcl_core @ v0.3.0`.
- [x] Move the `(ka) → physical` unit conversions into `dcl_core.calibration` —
      none exist in code (exp_01/exp_02 are pure lattice-unit; the (ka)→physical
      mapping is analysis-only in `notes/falsifiability_and_sme_mapping.md`).
      Recorded the calibration-discipline contract in the pin file + CLAUDE.md so
      `exp_03` (first place physical units appear) imports from `dcl_core.calibration`.
- [x] Keep §2.3 / test #5 (E+B dispersion-order) framed as the open GPU-bound
      question — done via companion handoff
      `2026-07-07-paper04-gauge-Q-tensor-framing-correction` (also consumed).

**Both:** flip `status: consumed` (and fill `consumed_by`/`consumed_at`) once your
repo's item is done; if only one repo lands first, note which in the file and leave
the other open.

> **Partial-consume note (2026-07-09):** the **dcl-paper-04** half is complete
> (commit 3de7423). Status stays `open` for the **dcl-delta-p-min** half (pin bump
> + Bohr/hydrogen calibration-constant centralisation), which a delta-p-min focused
> session must still do.
