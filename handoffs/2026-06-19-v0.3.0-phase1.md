---
handoff: 2026-06-19-v0.3.0-phase1
from: dcl-core (focused)
to: PM
repo: dcl-core
branch: feat/v0.3.0-peierls-phase1
commits: [fb97929, 85a5a99]
pr: none
status: consumed
state: in-progress
semver: 0.2.2 -> 0.3.0 (MINOR, unreleased)
flags: [acceptance-test-4-confirms-backward-midpoint-sign]
decisions: [D4-backward-midpoint, version-bump-deferred]
consumed_by: PM
consumed_at: 2026-06-19
---

## Summary
v0.3.0 adds a background U(1) gauge field coupled to the core3d hop via the
Peierls substitution — the engine capability Paper IV's exp_03 (optical-axis
birefringence, gauge sector) needs. Requirements in
`docs/design/04_gauge_field_and_vacuum_response.md`; plan is phased. **Phase 1
(CPU Peierls core) is complete.** Backfilled from a chat handoff (the first
`/handoff` record).

## Shipped
- `fb97929` — feat: `HopOperator.step` gains optional `vector_potential`
  kwarg; `_hop_average` applies the per-link Peierls phase. New
  `tests/test_peierls.py` (8 tests): zero-field regression, U(1) Ward
  identity, A=1 exactness.
- `85a5a99` — docs: new LIVING design doc `docs/design/05_interaction_engine.md`,
  the v0.3.0 plan note, a Mermaid class diagram, CLAUDE.md pointers.

## Verification
Full suite 268 passed / 26 xfailed / 0 failed (the 26 xfails are pre-existing
Paper I engine drift, untouched). Reviewed by api-stability-reviewer (additive,
backward-compatible, MINOR → 0.3.0) and physics-naming-reviewer (refinements
applied).

## Remaining
- Phase 2: `uniform_B_potential` helper; thread the field through
  `TickScheduler`; tests #3 (B→0 quadratic) and #4 (Paper I Q-tensor {4,4,16},
  axis (1,1,-1)).
- Phase 3: exp_03 E+B susceptibility sweep + test #5 (birefringence-order
  verdict — the load-bearing physics result).
- Phase 4 (GPU-gated): RawKernel Peierls path + static-link-phase caching.
- Release (when Phases 2–3 green): bump `_version.py` + `CITATION.cff`,
  Zenodo deposit, tag, GitHub Release, then bump Paper IV's pin to `@v0.3.0`.

## Decisions & flags
- **`acceptance-test-4-confirms-backward-midpoint-sign` (FLAG).** The Peierls
  midpoint is implemented **backward** — `A_mid = ½(A(x)+A(x−v))`, matching the
  hop's gather-from-`x−v` direction — not the requirements doc's literal
  `A(x+v)`. Invisible to all Phase 1 tests (none probe the sign), so it is
  **unverified**: it fixes the B-field sign that **acceptance test #4** (Paper I
  Q-tensor vs App. B) must confirm. If #4 sign-flips, the midpoint direction is
  the prime suspect — do not chase it elsewhere first. This is the §0.5 sign
  trap made concrete.
- **`D4-backward-midpoint` (decision).** Logged in the plan note §1 D4.
- **`version-bump-deferred` (decision).** `_version.py`/`CITATION.cff`
  deliberately NOT bumped; v0.3.0 is not releasable until Phases 2–3 land.

## → Consumer actions
- [x] Board: annotated issue **#20** (title "018 … core3d v0.3.0 Peierls",
      repo `discrete-causal-lattice-project`, project 6) — Phase 1 complete,
      Phases 2–4 remaining; NOT closeable yet.
      (NB: the draft-file prefix `018` is in the issue *title*; the GitHub
      issue *number* is **#20**. exp_03 = #18. PM memory's #20 was correct.)
- [x] Memory: backward-midpoint convention recorded
      (`core3d-peierls-backward-midpoint.md`).
- [x] Recommend to focused Claude: record `A_mid` **direction**
      ("backward / gather-side"), not just "half-sum", in the provenance
      manifest's `run.fields[].A.A_mid` (plan note §3.5) so a sign bug is
      traceable.
- [x] Gate: Paper IV is NOT gated on this (gauge row = PASS-structural; v0.3.0
      is the confirmatory witness). Site engine essay stays deferred until
      v0.3.0 fully lands (Phases 2–3). [acknowledged — standing gate]
