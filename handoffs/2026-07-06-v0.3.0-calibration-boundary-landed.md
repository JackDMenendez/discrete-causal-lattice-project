---
handoff: 2026-07-06-v0.3.0-calibration-boundary-landed
from: dcl-core (focused)
to: PM
repo: JackDMenendez/dcl-core
branch: feature/v0.3.0-peierls-gauge
commits: [0de7553, 5f816ce, 0c7ab8f]   # Peierls (committed). Boundary work is UNCOMMITTED working tree — see flag 1.
pr: none
status: open
state: in-progress   # boundary landed on branch; commit + release cut still ahead (gated)
semver: 0.2.2 -> 0.3.0 (MINOR, UNRELEASED). Additive: dcl_core.calibration submodule + core3d Peierls exports. api-stability-reviewer verdict = MINOR; no removals/renames.
flags:
  - "UNCOMMITTED: the extensional/calibration-boundary deliverables are in the dcl-core WORKING TREE, not yet committed (design doc 05, src/dcl_core/calibration.py, tests/test_calibration_boundary.py, and edits to __init__.py / core3d/session.py / test_smoke.py / CLAUDE.md). The Peierls gauge work IS committed (5f816ce, 0c7ab8f). Whoever cuts v0.3.0 must commit the boundary first."
  - "RELEASE GATE HONORED (do not undo): the boundary is now IN PLACE on the branch, so the gate from handoff 2026-07-04 is satisfied — but the release itself is NOT cut and _version.py/CITATION.cff are NOT bumped (still 0.2.2). Do the version bump once, at release time, for the whole cut (Peierls + boundary together)."
  - "SEMVER WATCH ITEM (api-stability-reviewer): Calibration.length_metres bakes in the light-cone identity a = c*tick_seconds. That is a MODELING ASSUMPTION on a public method, not a unit conversion — if a future lattice sets spacing a independently of c, changing the formula is a BREAKING (MAJOR) change. The v0.3.0 release notes MUST state 'assumes a = c*tick_seconds' so a later change is recognised as MAJOR. Documented in the method docstring (.. warning::) and design doc 05 §4.1."
  - "core3d ships ZERO calibration constants (audit finding) and the boundary has exactly ONE free debt: tick_seconds. tests/test_calibration_boundary.py asserts len(CALIBRATION_DEBTS)==1 as a thesis tripwire — if a 2nd debt ever lands, that test fails ON PURPOSE; decide real-new-scale vs sneak, do not just bump the expected count."
  - "ENV DRIFT (not a dcl-core defect): the canonical .venv-win was missing scipy + pytest and had no editable dcl_core install; I ran `pip install -e . pytest` (pulled scipy 1.18, pytest 9.1) to verify. Possible wcde repo-setup gap for this venv — flagging for the wcde/PM track, not blocking."
decisions:
  - "SCOPE CONFIRMED: core3d-only NOW. `core` (released v0.2.2, pinned by Paper III/delta-p-min and Paper VI/Bell) deferred to its next MAJOR — retrofitting the wall there is a breaking change to a shipped, reproduce-pinned API. Logged as debt against core's next MAJOR. (Matches PM recommendation in handoff 2026-07-04.)"
  - "ENFORCEMENT CONFIRMED: lightest-viable — a dcl_core.calibration module + naming discipline + one boundary test. No typed-units dependency. (Matches PM recommendation.)"
  - "SEQUENCING: folded the boundary into the SAME cut as the unmerged Peierls branch (an explicitly-allowed option in handoff 2026-07-04), working on feature/v0.3.0-peierls-gauge. Merge-then-refactor was the alternative; fold-into-one-cut was chosen so v0.3.0 ships gauge coupling + the wall together."
consumed_by:
consumed_at:
---

## Summary
Return handoff consuming PM directive `2026-07-04-extensional-core-calibration-boundary`.
The extensional-core / calibration-boundary architecture is **landed on branch
`feature/v0.3.0-peierls-gauge`** (working tree). `core3d` is confirmed constant-free
(the engine emits only extensional quantities — ticks, hops, tokens, counts,
per-tick phase, lattice-unit positions); every physical-unit conversion is now
quarantined behind the new `dcl_core.calibration` submodule (Layer C), which carries
exactly **one** free calibration debt (`tick_seconds`). This satisfies the timing
gate (draw the wall while core3d is still unreleased → free reshape, not a break) and
sets up v0.3.0 to ship the Peierls gauge coupling and the calibration wall in one cut.

## Shipped
Committed (Peierls, prior):
- `0de7553` — core3d gauge-field requirements (design doc 04) + engine policy.
- `5f816ce` — core3d U(1) Peierls gauge coupling (`uniform_B_potential` + Peierls hop), CPU-first.
- `0c7ab8f` — doc 04 wording alignment.

Uncommitted working tree (this handoff's boundary work — **flag 1**):
- `docs/design/05_extensional_core_calibration_boundary.md` — three layers
  (extensional core / derived-extensional / calibration boundary), enforcement,
  the API audit, scope decision, release gate, downstream list.
- `src/dcl_core/calibration.py` — `Calibration` (frozen dataclass; `tick_seconds`),
  conversions `seconds`/`frequency_hz`/`energy_joule`/`length_metres`/`velocity_mps`
  from `tick_seconds` + CODATA `hbar`/`c` (measured, NOT debts), `CALIBRATION_DEBTS`.
- `tests/test_calibration_boundary.py` — 18 tests: one-debt assertion, AST no-leak
  sweep over core3d, one-directional import wall, conversion round-trips.
- `src/dcl_core/__init__.py` — re-export `calibration` (additive third submodule).
- `src/dcl_core/core3d/session.py` — `omega` docstring: name kept lattice-frame
  (radians/tick), SI energy reading pointed at the boundary. Docstring-only.
- `tests/test_smoke.py` — top-level `__all__` now `{__version__, core, core3d, calibration}`.
- `CLAUDE.md` — new CURRENT STATUS (2026-07-06) block.

## Verification
- Full suite: **green (pytest exit 0)**; only pre-existing xfails + one sympy-not-installed
  skip (environmental). New `test_calibration_boundary.py` (18) + `test_smoke.py` all pass.
- **api-stability-reviewer:** verdict **MINOR** (additive-only; no removals/renames/breaking
  signatures). Watch item raised → flag 3 (`length_metres` light-cone assumption).
- **physics-naming-reviewer:** **0 violations**. Two cosmetic notes; the actionable one
  (`:func:` → `:meth:` on the omega docstring pointer) is fixed. The other (`N_units`
  vs `n_units` capitalization in the session docstring) is pre-existing and left as-is
  to match the surrounding docstring.

## Remaining
- Commit the working-tree boundary deliverables on the branch (flag 1).
- At release time only: bump `_version.py` + `CITATION.cff` to `0.3.0` (flag 2); do the
  standard release flow (Zenodo deposit first, tag, GitHub Release). **Not yet — gated.**
- v0.3.0 release notes must include the `length_metres` "assumes a = c*tick_seconds"
  caveat (flag 3).
- Downstream (per directive, "not a big deal", done when each repo bumps its pin to
  v0.3.0): move ad-hoc physical-unit constants to `dcl_core.calibration` —
  dcl-delta-p-min/Paper III (Bohr/hydrogen calibration), dcl-paper-04 ((ka) → physical).
  Paper VI/Bell pins `core` (v0.2.2) so unaffected until core's MAJOR.
- `core`-side wall deferred to core's next MAJOR (decision above).

## Decisions & flags
See frontmatter — the load-bearing points are flags 1–4 and the three decisions.
Confirmations back to the PM: **scope = core3d-only now**, **enforcement = lightest
viable**, **sequencing = folded into the Peierls cut**. The audit's headline number for
the "one calibration number" thesis: **the boundary holds exactly one free debt
(`tick_seconds`); the engine holds zero constants**, and a CI test fails on purpose if a
second debt ever appears.

## → Consumer actions
- [ ] Board: on issue **#20** (`018 Core Software core3d v0.3.0`, project 6,
      `discrete-causal-lattice-project`), record that the extensional-core /
      calibration-boundary requirement from handoff 2026-07-04 has **landed on
      `feature/v0.3.0-peierls-gauge`** (design doc 05 + `dcl_core.calibration` + tests;
      audit = 0 engine constants / 1 boundary debt), and that the release gate is now
      satisfied. Note the three release-time flags (commit, version bump, `length_metres`
      caveat).
- [ ] Memory (dcl-core session dir): record the extensional/calibration-boundary
      architecture — `dcl_core.calibration` is the ONE physical-unit home; `tick_seconds`
      is the single free debt; the one-debt CI tripwire; and the `a = c*tick_seconds`
      MAJOR-watch on `length_metres`. Link `[[v1-release-gating]]`.
- [ ] Gate: do NOT cut v0.3.0 / bump `_version.py` until the working tree is committed
      and the release-notes caveat (flag 3) is written. The boundary itself is DONE.
- [ ] Relay to dcl-core focused (when v0.3.0 cuts): bump downstream pins and move their
      ad-hoc calibration constants into `dcl_core.calibration` (dcl-delta-p-min, dcl-paper-04).
- [ ] wcde/env track: check whether the canonical `.venv-win` should ship scipy + pytest +
      an editable dcl_core install (flag 5) — a possible repo-setup gap, not a dcl-core defect.
- [ ] Note: the sibling directive handoff `2026-06-27-entangled-pair-preparation-physics-gap`
      (to dcl-core) remains **open/unaddressed** — the author-owned entangled-pair birth-event
      physics question; not touched by this session.
