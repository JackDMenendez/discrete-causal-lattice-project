---
handoff: 2026-07-21-paper02-v2.0-published
from: dcl-paper-02-sm-derivation (focused)
to: PM
repo: JackDMenendez/dcl-paper-02-sm-derivation
branch: main
commits: [f90447e]
pr: none
status: consumed
state: complete
semver: v1.01 -> v2.0 (MAJOR, released). Scientific-correction release: the derivation's substrate is superseded, not merely annotated.
flags:
  - "GATE OVERRIDDEN BY THE AUTHOR — the paper-04 'shared O_h-average root' handoff WAS NEVER FILED and is still absent from handoffs/ as of 2026-07-21. Both 2026-07-18-papers-i-ii-zenodo-reversion-procedure and 2026-07-20-paper02-revision-notice-text gated Paper II's version choice AND its Zenodo cut on that handoff. The author overrode the gate directly on 2026-07-21, selecting v2.0 and supplying a pre-reserved DOI. Do NOT read Paper II v2.0 as evidence that the O_h-average-root question was answered — it is still open, and it still owes Paper II a verdict on how much of the SM derivation survives the successor geometry."
  - "AUTHOR'S POSITION IS STRONGER THAN THE PUBLISHED NOTICE. The in-PDF revision notice (author-approved verbatim) says results 'should be read conditionally'. The author's actual stated position, 2026-07-21: the derivation is OBSOLETE; the architecture left an optical axis that is observable but not observed; the lattice GEOMETRY WILL HAVE TO CHANGE; the successor architecture is NOT birefringent. The author deliberately kept the softer verbatim notice in the PDF and had it point to Paper IV, putting the escalation in the audit table and the Zenodo description instead. Anyone summarizing Paper II's status (website, claim map) should use the stronger framing, not the notice's wording."
  - "SU(3)-UNDER-NEW-GEOMETRY IS OPEN AND WAS EXPLICITLY DEFERRED. Paper II's colour SU(3) comes from a per-site C^3 index recording which of V_1, V_2, V_3 carried the most recent tick — colour space IS the three-diagonal set. If the successor architecture adds V_4, the natural reading is C^4 -> SU(4), not SU(3). Paper IV's parked escape hatch (four-orientation VACUUM while MATTER keeps its axis) would preserve colour, but that is unresolved foundational research. The author's ruling on 2026-07-21: 'we don't need to worry about it now.' v2.0 therefore makes NO claim either way. This is the single largest open question about how much of Paper II survives."
  - "g_3^2/g_2^2 = 3/2 IS DELIBERATELY LEFT OPEN, NOT WITHDRAWN. An earlier draft of the v2.0 deposit prose asserted the coupling ratio was conditional on the superseded substrate. The claim-auditor flagged this as unestablished, and it was corrected before the tag: the Q-tensor and the universal one-loop prefactor CANCEL in the ratio (which is exactly why the paper calls it independent of c), so the ratio may well survive the no-go — but the successor geometry could change the spectator counting it rests on. No changes-table row asserts either outcome. Do NOT propagate 'the coupling ratio is falsified' to the website or claim map."
  - "THE PUBLISHED ZENODO DESCRIPTION CARRIES THE UNCORRECTED WORDING. The author deposited BEFORE the coupling-ratio correction above was made, so the live record still says the ratio is conditional. The corrected text is in release_notes/zenodo_description.{txt,html} at commit f90447e. Zenodo permits description edits without minting a new DOI. This is the only place the overstatement is currently public."
  - "ABSTRACT AND CONCLUSION ARE KNOWINGLY UNRECONCILED. conclusion.tex:443-447 still hands the (1,1,-1) birefringence to experiment as an unconditional testable consequence, contradicting the changes table a few pages earlier in the same PDF; conclusion.tex:236-247 inherits the Q-tensor with no reference to its FAIL row; abstract.tex:22-25 states the coupling ratio unqualified. The author deferred this rework. The claim-auditor rates the conclusion item HIGH severity. It is shipped as-is in v2.0."
decisions:
  - "Author (2026-07-21): v2.0, not v1.1 — the derivation's substrate is superseded, so the bump is major. DOI 10.5281/zenodo.21477781 was pre-reserved by the author before the release."
  - "Author (2026-07-21): keep the in-PDF revision notice VERBATIM and let it direct the reader to Paper IV, which 'spells it all out', rather than restating the no-go in Paper II's own words. Escalation lives in the audit table and deposit prose instead."
  - "Author (2026-07-21): place the notice immediately BEFORE the abstract, overriding the stale `%% FRONT-MATTER NOTICES (after abstract per author preference)` comment still in main.tex. Matches Paper I v2.0."
  - "DOI convention: the notice and bibliography cite Paper IV's VERSION DOI 10.5281/zenodo.21435952, not the concept DOI 10.5281/zenodo.21435951 that the 2026-07-18 procedure handoff specified. This matches the same author-approved deviation already taken for Paper I v2.0 — consistent across both papers."
consumed_by: PM (dcl-website session) — website reconciled to Paper II v2.0 (page → v2.0/DOI 21477781 + release link + notice above abstract using the AUTHOR STRONGER framing; FAIL 1→2; tally 8 PASS + 3 PART + 2 FAIL; claim map gains the FAIL row + a Paper II v2.0 callout; colour C³→SU(3) under V₄ flagged as THE open question; g₃²/g₂²=3/2 recorded OPEN-not-falsified; provenance → Paper II v2.0) — all LOCAL/unpushed. Verified SM-chirality is PART (v1.0-deposit FAIL error NOT inherited) and no stale tally existed. O_h-average-root debt ROUTED to paper-04 as 2026-07-21-paper04-oh-root-handoff-still-owed. Zenodo description paste + template-hygiene issue left to author. Memory updated (DOI convention recorded).
consumed_at: 2026-07-21
---

## Summary
**Paper II v2.0 is published and released.** It is a scientific-correction
release: Paper IV's single-domain no-go supersedes the three-diagonal
substrate the SM-gauge derivation is carried out on. A=1 conservation is not
falsified and the algebraic results are unaffected, but the geometry must
change and the successor architecture is non-birefringent. This closes the
Paper II half of `2026-07-18-papers-i-ii-zenodo-reversion-procedure` and all
of `2026-07-20-paper02-revision-notice-text` — **both now consumed** — and
returns the DOI the PM needs for the website papers pages and claim map.
It also records that the gate those handoffs imposed was **overridden, not
satisfied**.

- **Zenodo v2.0:** [10.5281/zenodo.21477781](https://doi.org/10.5281/zenodo.21477781)
- **GitHub Release:** https://github.com/JackDMenendez/dcl-paper-02-sm-derivation/releases/tag/v2.0
- **Prior version:** v1.01, 10.5281/zenodo.20292158 (preserved, not withdrawn)
- **Falsifying result:** Paper IV v1.0, 10.5281/zenodo.21435952

## Shipped
- `f90447e` — v2.0 scientific correction. Front-matter revision notice
  (verbatim) immediately before the abstract + changes table on its own page;
  audit-table row *Single-domain gauge viability → FAIL*; central-claim tally
  corrected `8 PASS + 3 PART + 1 FAIL` → `8 PASS + 3 PART + 2 FAIL`; Paper IV
  added to the bibliography (now reference **[1]**, since the notice precedes
  the abstract and `unsrt` numbers in citation order); title page and
  `CITATION.cff` bumped to v2.0 with the new DOI; all four Zenodo deposit
  files updated for v2.0, each gaining a rich-text HTML variant; build-system
  repair (below).

Tag `v2.0` pushed; GitHub Release published (not draft/prerelease) with
`dcl-paper-02-sm-derivation-v2.0.pdf` (575,579 bytes) attached.

## Verification
- **Build:** 34 pages; final pdflatex pass has **0 undefined references and 0
  errors**; bibtex clean. Verified from a clean tree at the committed state.
- **Front matter:** notice p1, changes table p2, abstract p3 — confirmed by
  extracting text from the built PDF, not by inspection of source alone.
- **Tests:** 5 passed (`pytest tests/`, canonical `.venv-win`). **No CI
  workflow exists in this repo** — `.github/workflows/` is absent, so the
  release-flow step "CI green" had nothing to gate on.
- **claim-auditor agent:** independently recounted the audit table and
  confirmed `8 PASS + 3 PART + 2 FAIL` matches the actual rows; confirmed the
  changes-table row mapping satisfies the 07-20 handoff's instruction;
  confirmed the new FAIL row's substance against Paper IV's own audit table.
  It **caught one real overstatement**, which was corrected before tagging —
  see the coupling-ratio flag.
- **Semver:** MAJOR. The substrate of the derivation is superseded and a claim
  is withdrawn as constructed; this is not a metadata or annotation release.

## Remaining
- **Paper II's SM conclusions are NOT reworked.** Abstract and conclusion still
  present the coupling ratio and the birefringence as unconditional testable
  consequences. Deferred by the author; shipped as-is.
- **The O_h-average-root question is still unanswered** and still owes Paper II
  a verdict — specifically whether colour `C^3 → SU(3)` survives a successor
  geometry that adds `V_4`.
- **The live Zenodo description needs a paste** to pick up the coupling-ratio
  correction (metadata edit; no new DOI required).
- `common.mak` still sets `VENV := .venv-ucrt64`, which the global environment
  doc says was deleted (canonical is `.venv-win`). Does not affect
  `make paper`; will bite `make env` / `make tests`. Left alone as out of
  scope for this release.

## Decisions & flags
See frontmatter — six flags, all load-bearing. The two a careless reader is
most likely to drop:

1. **The gate was overridden, not cleared.** Two separate handoffs said "do
   NOT cut until the O_h-average-root handoff lands." It never landed. The
   author decided anyway. Paper II v2.0 existing is therefore *not* evidence
   that the O_h question was resolved — the opposite: it is still open and now
   has a published paper depending on its answer.
2. **The coupling ratio is open, not dead.** The natural careless summary of
   "single-domain no-go corrects Paper II" is "Paper II's `g_3^2/g_2^2 = 3/2`
   prediction is falsified." That is **not** what v2.0 says, and the draft
   prose that implied it was corrected before the tag. The Q-tensor cancels in
   the ratio.

Also worth the PM's attention: a **pre-existing error was found and fixed** in
the v1.0 Zenodo description, which claimed two FAIL rows including
*SM-chirality coupling alignment*. That row is `PART`, not `FAIL`. If the
website or claim map inherited that count from the v1.0 deposit text, it is
wrong there too.

**Build-system repair (non-scientific, but it had a live data-loss bug).**
`make` wrote to `build/`/`stage/` while the repo's real directories were
`.build/`/`.stage/`, so the targets and the on-disk layout had silently
drifted apart. Repointed both, added a `VERSION` variable, and `promote` now
emits the version-stamped `.stage/<DOC_TITLE>-v<VERSION>.pdf`. Two latent
bugs fixed in passing: `.build/` was **never gitignored** (one `git add -A`
from being committed), and `make clean` deleted `stage_dir` — which, once
repointed at the durable archive, would have destroyed **every released PDF**.
`clean` now removes `.build/` only. Other DCL paper repos derived from the
same template may carry the identical defects; `dcl` (Paper I) still has
`build_dir := build` / `stage_dir := stage` in its `common.mak`.

## → Consumer actions
- [ ] **Website:** update the Paper II entry on the dcl-website papers page to
      v2.0, DOI `10.5281/zenodo.21477781`, and link the GitHub Release. Paper I
      v2.0 is `10.5281/zenodo.21464971`.
- [ ] **Claim map:** record Paper II's *Single-domain gauge viability → FAIL*
      row and the corrected tally `8 PASS + 3 PART + 2 FAIL`. If the map
      inherited "two FAIL rows including SM-chirality alignment" from the v1.0
      deposit text, **correct it** — chirality alignment is `PART`.
- [ ] **Claim map — do NOT mark `g_3^2/g_2^2 = 3/2` as falsified.** Record it
      as *open under the successor geometry*, per the flag above.
- [ ] **Memory:** record the DOI convention now settled across Papers I and II
      — revision notices and bibliographies cite Paper IV's **version** DOI
      `10.5281/zenodo.21435952`, not the concept DOI, deliberately and with
      author approval.
- [ ] **Gate / route:** the paper-04 **O_h-average-root handoff is still
      unfiled**. Route it to the paper-04 session; it now has a *published*
      Paper II v2.0 depending on its verdict, specifically on whether colour
      `C^3 → SU(3)` survives a `V_4`-inclusive geometry.
- [ ] **Author task (not PM):** paste the corrected description from
      `release_notes/zenodo_description.{txt,html}` into the live Zenodo v2.0
      record to pick up the coupling-ratio fix.
- [ ] **Template hygiene:** consider filing a wcde/template issue for the
      `build/` vs `.build/` drift, the unignored `.build/`, and the
      `make clean` archive-deletion bug — `dcl` (Paper I) still carries all
      three.
